<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="/js/home/index.js" defer></script>
    <script src="/js/home/index.ctrl.js" defer></script>
    <link rel="stylesheet" href="/css/home/list.css">
    <title>Document</title>
  </head>
  <body>
    <form id="list">
    <table align="center">
      <tr align ="left">
        <td><h2>To Do List</h2></td>
      </tr>
      <tr>
        <td><input type="text" id="text" class ="input1" /></td>
        <td>
          <a id="add" class ="button">+</a>
        </td>
      </tr>
    </table>
    <hr>
    <!--나중에 수정하기 ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ-->
      <table align="center">
        <%for(let i = 0; i < data.length; i++){ %>
        <tr>
          <td>
            <center>
              <input type="checkbox" id="<%=data[i].id%>%>" name = "is_check" onClick="updateCheck(this)"
              <%= data[i].is_check ? '' : 'checked' %>/>
             
              <%if(data[i].is_check){%>
                <%=data[i].description%>
                <%}else{%>
                <strike><%=data[i].description%></strike>
                  <%}%>
              <a id="<%=i%>"" name ="update" onClick ="updateText(this)">[수정]</a>
              <!--나중에 수정 누르면 칸 나오고 확인 눌러야 업데이트 되도록 수정하기 -->
              <a id="<%=data[i].id%>" name="delete" onClick="deleteList(this)">
              [삭제]
              <hr/>
            </center>
          </td>
        </tr>
        <%}%>
        <!-- ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ-->
      </table>
    </form>
  </body>
</html>
