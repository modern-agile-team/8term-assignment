// 계산식 입력 시 계산하는 함수
// 입력버튼 누를 시 결과부분에 결과값 나오는 함수

alert("입력되었습니다.");

// 값을 받아내는 작업
// 판별하는 작업
// 값에 따라 연산하는 작업

function inputBtn() {}
function result() {
    document.getElementById("  ").innerHTML = "  ";
}
//exit 입력 시 result는 없고 종료 경고창 출력
function exit() {
    if ()
}